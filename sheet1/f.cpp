#include<bits/stdc++.h>
using namespace std;

int main(){

    long long a,b;
    cin>>a>>b;
    cout<<(a%10)+(b%10);
    

    // (X%10) returns the last digit
    // (X%100) returns the last 2 digits and so on
    // (X%n) returns a number from from 0 to n-1
    return 0;
}