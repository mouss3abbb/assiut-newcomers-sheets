#include<bits/stdc++.h>
using namespace std;

int main(){

    string name;
    // Reading the name as input
    cin>>name;

    // Printing the required output, note for the space after comma
    cout<<"Hello, "<<name;
    return 0;
}